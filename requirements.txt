# Minimum supported versions. Deliberately floors rather than hard pins: the
# code uses no version-specific API, and pinning to exact older releases can
# fail to build on a newer interpreter than the one we developed on.
#
# Validated end to end on Python 3.13.11 with:
#   pandas 3.0.5, numpy 2.4.6, scikit-learn 1.9.0, scipy 1.18.1,
#   openpyxl 3.1.5, matplotlib 3.11.1, seaborn 0.13.2, pytest 9.1.1

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
openpyxl>=3.1.0

# Charts. run.py degrades gracefully without matplotlib (--skip-charts).
matplotlib>=3.7.0
seaborn>=0.12.0

# Test suite only; not needed to produce the deliverables.
pytest>=7.0.0
